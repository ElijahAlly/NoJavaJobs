# * Install required libraries to run spiders
# 1) Navigate to scrapy/jobscraper/ in terminal 
# 2) Use this command: 
# pip3 install -r requirements.txt

selenium==3.141.0
scrapy==2.11.0
pytz
python-jobspy
pandas
tqdm